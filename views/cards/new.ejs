<!DOCTYPE html>
<html>
<head>
  <title>Add New Card</title>
  <%- include("../partials/head") %>
</head>
<body>

<%- include("../partials/header") %>

<div class="container mt-5" style="max-width: 650px;">
  <div class="card shadow border-0">
    <div class="card-body">

      <h2 class="fw-bold mb-4 text-center">Add New Gift Card</h2>

      <form method="POST" action="/cards">
        
        <div class="mb-3">
          <label class="form-label">Card Brand</label>
          <input class="form-control" name="cardBrand" placeholder="Example: Amazon, Walmart, Starbucks" required>
        </div>

        <div class="mb-3">
          <label class="form-label">Card Number</label>
          <input class="form-control" name="cardNumber" placeholder="Enter card number" required>
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Balance ($)</label>
            <input class="form-control" name="balance" type="number" step="0.01" placeholder="0.00" required>
          </div>

          <div class="col-md-6 mb-3">
            <label class="form-label">Expiry Date</label>
            <input class="form-control" name="expiryDate" type="date">
          </div>
        </div>

        <div class="mb-3">
          <label class="form-label">Notes (optional)</label>
          <textarea class="form-control" name="notes" rows="3" placeholder="Store location, PIN number, etc."></textarea>
        </div>

        <button class="btn btn-success btn-lg w-100">Save Card</button>
      </form>

      <a href="/cards" class="btn btn-link w-100 mt-3">← Back to My Cards</a>
    </div>
  </div>
</div>

<%- include("../partials/footer") %>
</body>
</html>
